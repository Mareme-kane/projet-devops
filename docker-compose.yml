version: '3'

services:
  gestion-classes:
    build: ./gestion-classes
    ports:
      - "8001:80"
    networks:
      - app-network

  gestion-cours:
    build: ./gestion-cours
    ports:
      - "8002:80"
    networks:
      - app-network

  gestion-professeurs:
    build: ./gestion-professeurs
    ports:
      - "8003:80"
    networks:
      - app-network

  gestion-etudiants:
    build: ./gestion-etudiants
    ports:
      - "8004:80"
    networks:
      - app-network

  gestion-emploi:
    build: ./gestion-emploi
    ports:
      - "8005:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
